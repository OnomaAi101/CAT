#!/bin/bash
# Run the training script in main repo folder
# this assumes you are already using proper conda environment / virtualenv
python cat_train_all.py --tuning_config_path configs/cat_tuning_config_cat_statue_cat6.json